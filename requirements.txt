# Core ML libraries
torch>=2.0.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.25.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Evaluation
evaluate>=0.4.0
jiwer>=3.0.0  # For WER/CER metrics

# Utilities
numpy>=1.24.0
tqdm>=4.65.0
pyyaml>=6.0.0  # For benchmark config files
jsonlines>=3.0.0  # For JSONL metadata files

# Optional: for tensorboard logging
tensorboard>=2.14.0

# Optional: for HuggingFace Hub (if pushing models)
huggingface-hub>=0.19.0

